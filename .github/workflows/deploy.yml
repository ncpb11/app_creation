name: deploy

on:
  push:
    branches:
      -main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      run: |
        echo "deploying to the staging"
        echo "deplying to the production"
